<Page
    x:Class="GalgameManager.Views.GalgameSourcePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:converter="using:GalgameManager.Helpers.Converter"
    xmlns:control="using:GalgameManager.Views.Control"
    xmlns:views="using:GalgameManager.Views"
    xmlns:viewModels="using:GalgameManager.ViewModels"
    xmlns:behaviors="using:CommunityToolkit.WinUI.Behaviors"
    xmlns:sources="using:GalgameManager.Models.Sources"
    xmlns:enums="using:GalgameManager.Enums"
    xmlns:cmtk="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d"
    x:Name="Page"
    NavigationCacheMode="Enabled">

    <Page.Resources>
        <converter:EnumToStringConverter x:Key="EnumStringConverter" />
        <converter:ImagePathConverter x:Key="ImagePathConverter" />
        <converter:EnumEqualsConverter x:Key="EnumEqualsConverter"/>

        <MenuFlyout Placement="Right" x:Key="GameItemFlyout">
            <MenuFlyoutItem x:Uid="GalgameSourcePage_GamesTable_Edit"
                           Command="{x:Bind ViewModel.EditGameCommand}"
                           CommandParameter="{Binding}">
                <MenuFlyoutItem.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE70F;" />
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem x:Uid="GalgameSourcePage_GamesTable_OpenInExplorer"
                            Command="{x:Bind ViewModel.OpenGameInExplorerCommand}"
                            CommandParameter="{Binding}">
                <MenuFlyoutItem.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8B7;" />
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem x:Uid="GalgameSourcePage_GamesTable_CopyName"
                           Command="{x:Bind ViewModel.CopyGameNameCommand}"
                           CommandParameter="{Binding}">
                <MenuFlyoutItem.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8C8;" />
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutItem x:Uid="GalgameSourcePage_GamesTable_CopyPath"
                           Command="{x:Bind ViewModel.CopyGamePathCommand}"
                           CommandParameter="{Binding}">
                <MenuFlyoutItem.Icon>
                    <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE8C8;" />
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
            <MenuFlyoutSeparator/>
            <MenuFlyoutItem x:Uid="GalgameSourcePage_GamesTable_Delete"
                           Command="{x:Bind ViewModel.DeleteSingleGameCommand}"
                           CommandParameter="{Binding}">
                <MenuFlyoutItem.Icon>
                    <FontIcon Glyph="&#xE74D;" Foreground="Red"/>
                </MenuFlyoutItem.Icon>
            </MenuFlyoutItem>
        </MenuFlyout>
    </Page.Resources>
    
    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="SizeChanged">
            <core:InvokeCommandAction Command="{x:Bind ViewModel.PageSizeChangedCommand}"/>
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>
    
    <Grid Margin="0 0 0 10">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <RelativePanel Grid.Row="0">
                <TextBlock Text="{x:Bind ViewModel.Item.Name, FallbackValue={}, Mode=OneWay}"
                           Style="{ThemeResource TitleTextBlockStyle}"
                           MaxWidth="{x:Bind ViewModel.TitleMaxWidth, Mode=OneWay}" />
                <CommandBar RelativePanel.AlignRightWithPanel="True" Background="Transparent" IsOpen="False"
                            DefaultLabelPosition="Right" Margin="{StaticResource CommandBarMargin}">
                    <!-- 扫描游戏 -->
                    <AppBarButton Icon="Refresh"
                            x:Uid="GalgameFolderPage_Refresh"
                                  AllowFocusOnInteraction="True"
                                  Command="{x:Bind ViewModel.ScanAllCommand}"
                                  IsEnabled="{x:Bind ViewModel.Item.IsSourceScanable, FallbackValue=False, Mode=OneWay}">
                        <AppBarButton.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control"
                                    Key="R"/>
                        </AppBarButton.KeyboardAccelerators>
                    </AppBarButton>
                    <!-- 从压缩包添加游戏 -->
                    <AppBarButton x:Uid="GalgameFolderPage_AddFromZip"
                                  Command="{x:Bind ViewModel.AddGalFromZipCommand}"
                                  AllowFocusOnInteraction="True">
                        <AppBarButton.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xF012;" />
                        </AppBarButton.Icon>
                        <AppBarButton.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="A"/>
                        </AppBarButton.KeyboardAccelerators>
                    </AppBarButton>
                    <!-- 删除游戏 -->
                    <AppBarButton x:Uid="GalgameFolderPage_Delete"
                                  IsEnabled="{x:Bind ViewModel.IsDownloadFromNameVisible, Mode=OneWay}"
                                  Command="{x:Bind ViewModel.DeleteGameCommand}"
                                  AllowFocusOnInteraction="True">
                        <AppBarButton.Icon>
                            <FontIcon FontFamily="Segoe Fluent Icons"
                                    Glyph="&#xE74D;"/>
                        </AppBarButton.Icon>
                    </AppBarButton>
                    <!-- 下载游戏数据 -->
                    <AppBarButton x:Uid="GalgameFolderPage_DownloadSelectedInfoFromId"
                        Icon="Download" Label="{x:Bind ViewModel.UiDownloadInfo, Mode=OneWay}" 
                                  Command="{x:Bind ViewModel.GetInfoFromRssCommand}" 
                                  AllowFocusOnInteraction="True" />
                    <!-- 从游戏名下载游戏数据 -->
                    <AppBarButton Icon="Download" x:Uid="GalgameFolderPage_DownloadSelectedInfoFromName"
                                  IsEnabled="{x:Bind ViewModel.IsDownloadFromNameVisible, Mode=OneWay}"
                                  Command="{x:Bind ViewModel.GetInfoFromRssCommand}" 
                                  CommandParameter="True"
                                  AllowFocusOnInteraction="True"/>
                    <!-- 添加排序按钮 -->
                    <AppBarButton Icon="Sort" x:Uid="LibraryPage_Sort">
                        <AppBarButton.Flyout>
                            <MenuFlyout Placement="Bottom">
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByName" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.NameSortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Name}"/>
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByLastPlay" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.LastPlaySortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=LastPlay}"/>
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByDeveloper" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.DeveloperSortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Developer}"/>
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByRating" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.RatingSortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Rating}"/>
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByReleaseDate" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.ReleaseDateSortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=ReleaseDate}"/>
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByAddTime" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.AddTimeSortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=AddTime}"/>
                                <RadioMenuFlyoutItem x:Uid="LibraryPage_SortByPath" 
                                                   Command="{x:Bind ViewModel.SortCommand}" 
                                                   CommandParameter="{x:Bind ViewModel.PathSortKey}"
                                                   IsChecked="{x:Bind ViewModel.CurrentSortKey, Mode=OneWay, Converter={StaticResource EnumEqualsConverter}, ConverterParameter=Path}"/>
                                <MenuFlyoutSeparator/>
                                <ToggleMenuFlyoutItem x:Uid="LibraryPage_SortDescending"
                                                      Command="{x:Bind ViewModel.ApplySortingCommand}"
                                                      IsChecked="{x:Bind ViewModel.SortDescending, Mode=TwoWay}"/>
                            </MenuFlyout>
                        </AppBarButton.Flyout>
                        <AppBarButton.KeyboardAccelerators>
                            <KeyboardAccelerator Modifiers="Control" Key="S"/>
                        </AppBarButton.KeyboardAccelerators>
                    </AppBarButton>
                    <CommandBar.SecondaryCommands>
                        <!-- 打开Log文件 -->
                        <AppBarButton x:Uid="GalgameFolderPage_Log" 
                                      IsEnabled="{x:Bind ViewModel.LogExists, Mode=OneWay}"
                                      Command="{x:Bind ViewModel.ViewLogCommand}"
                                      AllowFocusOnInteraction="True">
                            <AppBarButton.Icon>
                                <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE81C;" />
                            </AppBarButton.Icon>
                        </AppBarButton>
                    </CommandBar.SecondaryCommands>
                    <interactivity:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="SizeChanged">
                            <core:InvokeCommandAction Command="{x:Bind ViewModel.CommandBarSizeChangedCommand}"/>
                        </core:EventTriggerBehavior>
                    </interactivity:Interaction.Behaviors>
                </CommandBar>
            </RelativePanel>

            <!-- 库路径 -->
            <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="15">
                <TextBlock Text="{x:Bind ViewModel.Item.Path, FallbackValue={}}"
                           VerticalAlignment="Center" />
                <Border Background="{ThemeResource  ControlAltFillColorSecondaryBrush}"
                        BorderThickness="0"
                        CornerRadius="6"
                        VerticalAlignment="Center">
                    <TextBlock
                        Text="{x:Bind ViewModel.Item.SourceType, FallbackValue={}, Converter={StaticResource EnumStringConverter}}"
                        Style="{ThemeResource SmallTextStyle}"
                        MaxWidth="{x:Bind ViewModel.TitleMaxWidth, Mode=OneWay}"
                        Padding="10, 2" />
                </Border>
            </StackPanel>

            <Grid Grid.Row="2" Margin="0 20 0 0">
                <ListView ItemsSource="{x:Bind ViewModel.Galgames}"
                          SelectionMode="Multiple">
                    <ListView.Header>
                        <StackPanel Margin="0 0 20 0">
                            <control:SettingExpander x:Uid="GalgameSourcePage_Setting"
                                                     SettingIcon="&#xE713;" IsExpanded="False">
                                <control:SettingExpander.SettingContent>
                                    <StackPanel Spacing="13">
                                <!-- 库名 -->
                                <control:Panel>
                                    <control:Setting x:Uid="GalgameSourcePage_Setting_Name">
                                        <TextBox
                                            Text="{x:Bind ViewModel.Item.Name, Mode=TwoWay, FallbackValue={x:Null}}"
                                            MinWidth="150" />
                                    </control:Setting>
                                </control:Panel>
                                <!-- 封面图 -->
                                <control:Panel>
                                    <control:Setting x:Uid="GalgameSourcePage_Setting_Image"
                                                     Description="{x:Bind ViewModel.ImagePathDes, Mode=OneWay}">
                                        <StackPanel Orientation="Horizontal" Spacing="10">
                                            <Button x:Uid="ResetToDefault" Command="{x:Bind ViewModel.SetImagePathCommand}">
                                                <Button.CommandParameter>
                                                    <x:Boolean>True</x:Boolean>
                                                </Button.CommandParameter>
                                            </Button>
                                            <Button x:Uid="Select" Command="{x:Bind ViewModel.SetImagePathCommand}">
                                                <Button.CommandParameter>
                                                    <x:Boolean>False</x:Boolean>
                                                </Button.CommandParameter>
                                            </Button>
                                        </StackPanel>
                                    </control:Setting>
                                </control:Panel>
                                <!-- 启动时自动扫描 -->
                                <control:Panel Visibility="{x:Bind ViewModel.Item.CanChangeScanOnStart}">
                                    <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_AutoScan"
                                        IsOn="{x:Bind ViewModel.Item.ScanOnStart, Mode=TwoWay, FallbackValue=False}" />
                                </control:Panel>
                                <!-- 启动时检查库和游戏是否存在 -->
                                <control:Panel Visibility="{x:Bind ViewModel.Item.CanChangeCheckOnStart}">
                                    <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_CheckOnStart"
                                        IsOn="{x:Bind ViewModel.Item.CheckOnStart, Mode=TwoWay, FallbackValue=True}" />
                                </control:Panel>
                                <!-- 自动添加/取消托管游戏 -->
                                <control:Panel Visibility="{x:Bind ViewModel.Item.CanChangeDetect}">
                                    <StackPanel Spacing="12">
                                        <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_Auto"
                                            IsOn="{x:Bind ViewModel.Item.Detect, Mode=TwoWay, FallbackValue=False}" />
                                        <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_Auto_Add"
                                            IsOn="{x:Bind ViewModel.Item.DetectFolderAdd, Mode=TwoWay, FallbackValue=False}" 
                                            Visibility="{x:Bind ViewModel.Item.Detect, Mode=OneWay, FallbackValue=0}"/>
                                        <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_Auto_Remove"
                                            IsOn="{x:Bind ViewModel.Item.DetectFolderRemove, Mode=TwoWay, FallbackValue=False}" 
                                            Visibility="{x:Bind ViewModel.Item.Detect, Mode=OneWay, FallbackValue=0}"/>
                                    </StackPanel>    
                                </control:Panel>
                                <!-- 保存元数据备份 -->
                                <control:Panel Visibility="{x:Bind ViewModel.Item.CanChangeSaveMetaBackup}">
                                    <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_SaveMetaBackup"
                                        IsOn="{x:Bind ViewModel.SaveMetadata, Mode=TwoWay, FallbackValue=False}" />
                                </control:Panel>
                                 <!-- 不扫描路径列表 -->
                                <control:Panel Visibility="{x:Bind ViewModel.Item.IsSourceScanable}">
                                    <StackPanel>
                                        <control:Setting x:Uid="GalgameSourcePage_Setting_DontScanPaths">
                                            <Button x:Uid="GalgameSourcePage_Setting_DontScanPaths_Add"
                                                    Command="{x:Bind ViewModel.AddDontScanPathCommand}"/>
                                        </control:Setting>
                                        <ItemsRepeater ItemsSource="{x:Bind ViewModel.DontScanPaths}"
                                                       Margin="0 10 0 0">
                                            <ItemsRepeater.ItemTemplate>
                                                <DataTemplate x:DataType="x:String">
                                                    <Grid Padding="8, 3" CornerRadius="5"
                                                          Background="{ThemeResource SubtleFillColorTertiaryBrush}"
                                                          ColumnDefinitions="Auto, Auto" ColumnSpacing="5">
                                                        <TextBlock Text="{x:Bind Mode=TwoWay}"
                                                                   Style="{StaticResource DescriptionTextStyle}"/>
                                                        <Button Grid.Column="1" x:Uid="Delete"
                                                                Background="Transparent"
                                                                Padding="0" CornerRadius="10"
                                                                Click="ButtonBase_OnClick" Tag="{x:Bind}"
                                                                VerticalAlignment="Center" HorizontalAlignment="Center">
                                                            <FontIcon FontFamily="Segoe Fluent Icons" Glyph="&#xE894;"
                                                                      FontSize="10"/>
                                                        </Button>
                                                    </Grid>
                                                </DataTemplate>
                                            </ItemsRepeater.ItemTemplate>
                                            <ItemsRepeater.Layout>
                                                <cmtk:WrapLayout Orientation="Horizontal" 
                                                                 HorizontalSpacing="20" VerticalSpacing="5"/>
                                            </ItemsRepeater.Layout>
                                        </ItemsRepeater>
                                    </StackPanel>
                                </control:Panel>
                                      
                                    </StackPanel>
                                </control:SettingExpander.SettingContent>
                            </control:SettingExpander>
                            <!-- 切换是否显示子库中的游戏 -->
                            <views:SettingToggleSwitch x:Uid="GalgameSourcePage_Setting_IncludeSubSources"
                                Margin="0,12,0,8"
                                IsOn="{x:Bind ViewModel.IncludeSubSources, Mode=TwoWay}" />
                        </StackPanel>
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="sources:GalgameAndPath">
                            <Grid>
                                <!-- 透明控件，确保右键时射线检测能够打到Item上 -->
                                <Rectangle Fill="Transparent" /> 
                                <Grid ColumnDefinitions="Auto, *" Margin="0, 7">
                                    <Button Grid.Column="0" Margin="0 0 10 0"
                                            Style="{StaticResource TransparentButtonWithHover}"
                                            IsHitTestVisible="False">
                                        <Grid CornerRadius="10">
                                            <Rectangle Height="70" Width="50">
                                                <Rectangle.Fill>
                                                    <ImageBrush
                                                        ImageSource="{x:Bind Galgame.ImagePath.Value,
                                                        Mode=OneWay,
                                                        Converter={StaticResource ImagePathConverter},
                                                        FallbackValue={x:Null}}"
                                                        Stretch="UniformToFill" />
                                                </Rectangle.Fill>
                                            </Rectangle>
                                        </Grid>
                                    </Button>
                                    <StackPanel Grid.Column="1" Spacing="3">
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock x:Uid="GalgameSourcePage_GamesTable_Name"
                                                       Style="{StaticResource DescriptionTextStyle}"
                                                       VerticalAlignment="Center" />
                                            <TextBox Text="{x:Bind Galgame.Name.Value, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                                                     MinWidth="450"
                                                     HorizontalAlignment="Left"
                                                     VerticalAlignment="Center" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock x:Uid="GalgameSourcePage_GamesTable_Path"
                                                       Style="{StaticResource DescriptionTextStyle}"
                                                       VerticalAlignment="Center" />
                                            <TextBlock Text="{x:Bind Path}"
                                                       Style="{StaticResource DescriptionTextStyle}"
                                                       VerticalAlignment="Center" />
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Spacing="5">
                                            <TextBlock x:Uid="GalgameSourcePage_GamesTable_RssType"
                                                       Style="{StaticResource DescriptionTextStyle}"
                                                       VerticalAlignment="Center" />
                                            <control:ComboBoxWithoutNull
                                                SelectedItem="{x:Bind Galgame.RssType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                ItemsSource="{x:Bind RssTypes}"
                                                VerticalAlignment="Center" />
                                            <TextBlock Text="ID"
                                                       Style="{StaticResource DescriptionTextStyle}"
                                                       VerticalAlignment="Center" />
                                            <TextBox MinWidth="350"
                                                     Text="{x:Bind Galgame.Id, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                     VerticalAlignment="Center" />
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                                <Grid.ContextFlyout>
                                    <StaticResource ResourceKey="GameItemFlyout"/>
                                </Grid.ContextFlyout>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <interactivity:Interaction.Behaviors>
                        <core:EventTriggerBehavior EventName="SelectionChanged">
                            <core:InvokeCommandAction Command="{x:Bind ViewModel.SelectionChangedCommand}" />
                        </core:EventTriggerBehavior>
                    </interactivity:Interaction.Behaviors>
                    <ListView.ItemContainerStyle>
                        <Style BasedOn="{StaticResource DefaultListViewItemStyle}"
                               TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
            </Grid>
        </Grid>

        <Grid
            Margin="25 10 25 100 "
            Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}"
            Visibility="{x:Bind ViewModel.IsUnpacking, Mode=OneWay}">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ProgressRing
                Value="{x:Bind ViewModel.ProgressValue, Mode=OneWay}"
                Background="LightGray"
                IsActive="True"
                IsIndeterminate="False"
                Width="100"
                Height="100"
                HorizontalAlignment="Center"
                VerticalAlignment="Center" />
            <TextBlock Grid.Row="1"
                       Text="{x:Bind ViewModel.ProgressMsg, Mode=OneWay}"
                       Margin="0 -80 0 50"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top" />
        </Grid>
    </Grid>

</Page>
