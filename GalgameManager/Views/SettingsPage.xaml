<Page
    x:Class="GalgameManager.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="using:GalgameManager.Views"
    xmlns:helpers="using:GalgameManager.Helpers"
    xmlns:xaml="using:Microsoft.UI.Xaml"
    xmlns:converters="using:CommunityToolkit.WinUI.UI.Converters"
    mc:Ignorable="d">
    <Page.Resources>
        <helpers:EnumToBooleanConverter x:Key="EnumToBooleanConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>


    <Grid>
        <ScrollViewer HorizontalScrollMode="Disabled">
            <StackPanel Margin="0 10 0 0">

                <local:SettingExpander SettingTitle="外观"
                                       SettingDescription="切换深色或浅色模式"
                                       SettingIcon="&#xE790;">
                    <local:SettingExpander.SettingContent>
                        <StackPanel>
                            <RadioButton
                                x:Uid="Settings_Theme_Light"
                                Command="{x:Bind ViewModel.SwitchThemeCommand}"
                                IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Light, Mode=OneWay}"
                                FontSize="15"
                                GroupName="AppTheme">
                                <RadioButton.CommandParameter>
                                    <xaml:ElementTheme>Light</xaml:ElementTheme>
                                </RadioButton.CommandParameter>
                            </RadioButton>
                            <RadioButton
                                x:Uid="Settings_Theme_Dark"
                                Command="{x:Bind ViewModel.SwitchThemeCommand}"
                                IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Dark, Mode=OneWay}"
                                FontSize="15"
                                GroupName="AppTheme">
                                <RadioButton.CommandParameter>
                                    <xaml:ElementTheme>Dark</xaml:ElementTheme>
                                </RadioButton.CommandParameter>
                            </RadioButton>
                            <RadioButton
                                x:Uid="Settings_Theme_Default"
                                Command="{x:Bind ViewModel.SwitchThemeCommand}"
                                IsChecked="{x:Bind ViewModel.ElementTheme, Converter={StaticResource EnumToBooleanConverter}, ConverterParameter=Default, Mode=OneWay}"
                                FontSize="15"
                                GroupName="AppTheme">
                                <RadioButton.CommandParameter>
                                    <xaml:ElementTheme>Default</xaml:ElementTheme>
                                </RadioButton.CommandParameter>
                            </RadioButton>
                        </StackPanel>
                    </local:SettingExpander.SettingContent>
                </local:SettingExpander>

                <local:SettingExpander
                    SettingTitle="信息源"
                    SettingDescription="选择galgame信息获取源"
                    SettingIcon="&#xE701;">
                    <local:SettingExpander.SettingContent>
                        <StackPanel>
                            <RadioButton
                                Content="Bangumi"
                                GroupName="Info"
                                IsChecked="{x:Bind ViewModel.RssSelectBangumi, Mode=TwoWay}">
                            </RadioButton>
                            <TextBox
                                Visibility="{x:Bind ViewModel.RssSelectBangumi, Converter={StaticResource BoolToVisibilityConverter}, Mode=OneWay}"
                                Text="{x:Bind ViewModel.BangumiToken, Mode=TwoWay}"
                                PlaceholderText="输入你的个人令牌，在这里获得:https://next.bgm.tv/demo/access-token/create"
                                Margin="0 5 0 5">
                            </TextBox>
                            <RadioButton
                                Content="Bilibili"
                                GroupName="Info"
                                IsChecked="{x:Bind ViewModel.RssSelectBilibili, Mode=TwoWay}">
                            </RadioButton>
                            <RadioButton
                                Content="Moegirl"
                                GroupName="Info"
                                IsChecked="{x:Bind ViewModel.RssSelectMoegirl, Mode=TwoWay}">
                            </RadioButton>
                        </StackPanel>
                    </local:SettingExpander.SettingContent>
                </local:SettingExpander>


                <TextBlock x:Uid="Settings_About" Style="{ThemeResource SubtitleTextBlockStyle}" />

                <StackPanel Margin="{StaticResource XSmallTopMargin}">
                    <TextBlock Text="{x:Bind ViewModel.VersionDescription, Mode=OneWay}"
                               Style="{ThemeResource BodyTextBlockStyle}" />

                    <TextBlock
                        x:Uid="Settings_AboutDescription"
                        Margin="{StaticResource XSmallTopMargin}"
                        Style="{ThemeResource BodyTextBlockStyle}" />
                    <HyperlinkButton x:Uid="SettingsPage_PrivacyTermsLink"
                                     Margin="{StaticResource SettingsPageHyperlinkButtonMargin}" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>