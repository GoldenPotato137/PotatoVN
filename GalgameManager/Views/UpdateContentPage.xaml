<Page
    x:Class="GalgameManager.Views.UpdateContentPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:CommunityToolkit.WinUI.UI.Controls"
    mc:Ignorable="d">
    
    <Grid Margin="{ThemeResource PageButtonMargin}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" VerticalAlignment="Center" x:Uid="UpdateContentPage_Title"
                   Style="{ThemeResource SubtitleTextBlockStyle}"/>
        
        <CommandBar Grid.Row="1" Background="Transparent" IsOpen="False" DefaultLabelPosition="Right" 
                    Margin="0 -40 0 0">
            <AppBarButton Icon="Refresh" x:Uid="UpdateContentPage_Refresh"/>
        </CommandBar>
        
        <ScrollViewer Grid.Row="2" Margin="{StaticResource MediumTopMargin}">
            <controls:MarkdownTextBlock Text="{x:Bind ViewModel.Content, Mode=OneWay}"/>
        </ScrollViewer>
    </Grid>
    
</Page>